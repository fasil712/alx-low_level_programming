#include <stdio.h>

/**
 * main - The program that print "data" by using printf function
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
